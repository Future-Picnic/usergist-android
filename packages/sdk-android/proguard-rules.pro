# Library-level proguard rules (applied when the library itself is minified
# during release builds). Consumer apps also receive `consumer-rules.pro`.

-keepattributes *Annotation*, InnerClasses, Signature, Exceptions
